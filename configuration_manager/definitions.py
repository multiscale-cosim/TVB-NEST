import os

ROOT_DIR = os.path.dirname(os.path.abspath(__file__))  # Path to the Root directory
CONFIG_FILE = os.path.join(ROOT_DIR, 'configuration_settings.xml')  # Path to the configuration file
OUTPUT_DIR = os.path.join(ROOT_DIR, 'outputs')  # Path to the outputs directory